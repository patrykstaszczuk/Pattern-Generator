from setuptools import setup

setup(name='pattern_generator',
      version='0.1',
      description='Generate lines based on text',
      author='Patryk Staszczuk',
      packages=['pattern_generator'],
      install_requires=["pillow"],
      zip_safe=False)
